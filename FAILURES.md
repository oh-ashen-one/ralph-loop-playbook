# Failure Catalog

Every failure mode we hit across five projects, with the fix. Ordered roughly
by how much wall-clock each one burned before diagnosis.

| # | Symptom | Root cause | Fix |
|---|---------|-----------|-----|
| 1 | Thousands of "skip incomplete last file" over a whole day | 600s curl timeout killing generation mid-file — a SECOND model sharing the GPU had quartered tok/s | Raise timeout to 1800s; run ONE model instance at a time, ever |
| 2 | Same file re-emitted byte-identically forever; verify never runs | Story asked for too much; the tail file always truncated | Micro-stage: "emit EXACTLY ONE file this iteration"; `allowedFiles` |
| 3 | Verify fails on a file NOTHING imports | Model created a speculative file in an earlier round; its syntax error masks the real errors | Manager deletes dead unreferenced files (curation); story forbids recreating it |
| 4 | Every iteration fixes one file and breaks another | Fresh instances re-derive shared modules from priors, dropping exports siblings import | `protect` (required-symbol write rejection) + "additive-only" story rule |
| 5 | Verify wrapper itself throws bash syntax errors | Model reasoning prose was written over a harness tool as a FILE block | Planning-prose filter + write-protect `tools/`,`scripts/` |
| 6 | Loop "passes" a story but the app is obviously unfinished | Verify asserted too little | Verify = build + a grep per acceptance criterion; critique findings become permanent asserts |
| 7 | Story loops although every verify condition passes when run by hand | Model can't produce even one valid FILE block, so the runner never reaches verify | Manager runs the verify verbatim; exit 0 → flip passes (the judge already ruled) |
| 8 | iOS story can never pass | Model hand-writing project.pbxproj (impossible) | xcodegen: model emits project.yml, harness generates the project |
| 9 | xcodebuild always fails on a fresh box | Verify targets a named simulator; no runtimes installed | Build against `-sdk iphonesimulatorX.Y CODE_SIGNING_ALLOWED=NO` |
| 10 | All verifies fail after moving the loop to another machine | Verify wrapper rsync+ssh'd to a hostname that doesn't resolve there | Local-mode branch in the wrapper; run loop on the inference box |
| 11 | An error survives 4+ story rephrasings | The model's prior about a function's return type beats your instruction | Spell the contract in words a model can't misread ("returns a NUMBER, never a canvas; create a canvas and RETURN THE CANVAS") |
| 12 | 109MB binary blocks the GitHub push | An early iteration committed node_modules | .gitignore before iteration 1; `git filter-repo --invert-paths --path node_modules` to clean history |
| 13 | Two loops fight over one repo; work happens twice, states diverge | A pkill missed the middle process of the supervisor tree; the "dead" loop kept spawning | Kill watchdog FIRST, then supervisor, then iteration; verify with pgrep; keep loops on ONE machine |
| 14 | Model ignores "do not emit X" prose | Prose is a request, not law | Enforce in the harness (`allowedFiles`); prompts persuade, harnesses prevent |
| 15 | Visible reply empty, only reasoning tokens | Local server ignores `enable_thinking: false` | Assistant prefill `</think>\n` |
| 16 | "Polish" story makes the model rewrite the whole app | Vague scope invites re-derivation | Stories name files and end states; polish = list of concrete deltas |
| 17 | Model emits files at repo root / invents `relative/` dirs | Loose FILE-path handling | `safe_rel` guard; story states the directory; junk cleaned by manager |
| 18 | Watchdog fires false alarms after timeouts raised | Stale-threshold < iteration length | Keep dead-man threshold > max iteration time |
| 19 | Class-field comma syntax, tuple-Hashable, missing imports recur across projects | Small-model favorite mistakes | Put the per-stack landmine list in QWEN.md so every fresh instance is warned |
| 20 | Quality plateaus at "compiles but mid" | Nothing pushes past functional | The critique phase (CRITIC.md): provenance test, findings → stories, repeat to dry |
| 21 | Model emits pure deliberation, zero FILE blocks, for many iterations | A suspicious artifact in the snapshot (a 4-byte stub file from an old session) made it distrust the tree and freeze | Tell it the provenance in the story: "X is a draft stub, you are writing it for the first time, nothing can be lost" |
| 22 | Stories "pass" with metadata-only or even net-DELETION commits | Verify grep scoped to the whole repo — it matched the loop's own log/story text; the loop then optimized into the hole (reward hacking) | Scope every grep to source dirs only; never let logs/prd/tools be grep-visible to a verify; audit passed stories by re-running verifies after any judge fix |
| 23 | Syntactically-broken .gd file passes verify, gets committed, poisons every later story | Godot `--check-only` prints "Parse error" but **exits 0**; a headless boot with `--quit-after` also exits 0 with broken scripts | Never trust the tool's exit code — capture output and grep for `Parse error|SCRIPT ERROR|Failed to load script`; gate every `--file *.gd` through this before greps run |
| 24 | "Short pass" story green with zero pass code | `--grep pass` matched the substring inside `_game_paused` | Grep exact declarations (`func do_pass`), never bare words; one declaration-grep per acceptance criterion |
| 25 | Shrink-guard loyally protects a file that is itself garbage; model refuses every rewrite, loops for a day | The "known-good restore point" commit was ALREADY truncated mid-expression (a clobber had been committed as a feat); story said "keep every existing function exactly as-is" about functions the model literally could not see — its refusal was CORRECT | Validate a restore point before declaring it baseline (tail must parse; run the parse gate on it); when the model freezes, check whether the story is lying about the state of the tree — the contradiction is the rut |
| 26 | Model spends every iteration reasoning about machines, rsync, and missing assets instead of coding | Stale ops docs (HANDOFF.md with a pre-migration rsync/laptop narrative) were inside the source snapshot because `.md` is a source suffix | Exclude root-level ops docs (HANDOFF/README/AGENTS) from the snapshot; every sentence the model sees must be currently true |
| 27 | 61KB of reasoning_content, then 10-byte writes and "no FILE blocks", 15 attempts on one story | `/no_think` + `reasoning_effort` + `chat_template_kwargs.enable_thinking:false` are ALL ignored by LM Studio for this arch; hidden thinking ate the whole max_tokens budget | The `</think>` assistant prefill is the only suppressor that works (law 1); also raise max_tokens so residual thinking cannot starve an 11KB file emission |
| 28 | Model emits fake `<tool_call>` XML ("running" wc -l) instead of FILE blocks | Stacking `/no_think` ON TOP of the `</think>` prefill flipped the chat template into tool-calling mode | Use ONE thinking suppressor (the prefill), never stack them; tell the model in the story "you have NO tools; your reply is written straight to disk" |
| 29 | The single clean emission of the day rejected by the harness | Manager wrote a `protect` list from convention (`export`) for a file that never contained that symbol — an impossible gate | Derive protect lists by grepping the CURRENT file, never from what a file of that kind "should" contain |
| 30 | Chain fires the next project's loop while the previous one is unfinished | Chain scripts wait on process-exit (pgrep), and a loop also exits when it merely EXHAUSTS its iteration budget | Chain on state, not on exit: gate each stage on `jq '[.userStories[]|select(.passes|not)]|length' == 0`, retry the unfinished stage otherwise |
| 23 | Parse-error judge never fails (Godot) | Godot 4.7 headless exits 0 on parse errors | Judge by output grep (`SCRIPT ERROR|Parse Error`); smoke-test every new judge both ways before trusting it |
| 24 | Blender batch convert gets one garbage path | Unquoted file-list variable under zsh (no word-split) | Run staging scripts with bash shebangs; never inline the loop in a zsh ssh one-liner |
| 25 | Asset staging half-misses with guessed filenames | Curation lists written from memory, not `unzip -l`/`ls` | List the archive first; patch the curation list from real names; keep a MISS log line per absent file |
| 26 | Big-file stories thrash forever: every whole-file re-emission truncates | Files past ~180-200 lines exceed reliable single-shot emission (law 3) | Harness `### PATCH:` blocks (see HARNESS.md): exact-match search/replace with uniqueness + path guards; story mandates ONE patch; scaffold says "always PATCH files over ~200 lines" |
| 27 | Model's patches never apply ("search text not found") though the fix is right | It patched from MEMORY — the target file was truncated out of its snapshot, so the search text is a paraphrase | A patch story MUST pin its target files verbatim (allowedFiles → full-text in snapshot); raise the snapshot budget before adding patch stories |
| 28 | Correct patches silently unparsed | Model emits git-conflict syntax (`<<<<<<<`/`=======`, fenced) not the spec's bare 4-char markers | Harness bends to natural emission: tolerate `<{4,}`/`={4,}`/`>{4,}` and optional code fences in PATCH_RE |
| 29 | 20-minute 62KB replies deliberating about a harness error that is already fixed | Stale LAST_VERIFY text stays in every prompt after the manager fixes the underlying problem | After ANY manager-side fix, overwrite LAST_VERIFY with the truth ("fixed by manager, implement normally") and cycle the iteration; state files must never lie |
| 30 | Verify never runs though the right file landed on disk | Files were drafted in the reasoning stream; mid-stream flushes wrote them, but the final parse saw no blocks → "no files emitted" → judge skipped | Count mid-stream writes toward the iteration's written set |
| 31 | Green/red whiplash: same gate passes and fails on alternating runs | Verify builds and the manager's hand-builds and a dev server all share one build dir (.next); concurrent builds corrupt what the probe measures | ONE writer per repo, ever; gate against an isolated prod build+server on its own port; manager never builds while an iteration is mid-flight (SIGSTOP first) |
| 32 | Slot/loop watchdog reaps a healthy loop, or two loops run one repo | pgrep patterns contain path text that a relative `./scripts/...` invocation never puts in the cmdline | Invoke loops by ABSOLUTE path so patterns match; validate every pattern with pgrep against the live process at claim time |
| 33 | Same syntax error class survives QWEN.md warnings for 6+ iterations | Model reflexes (GDScript `var x := <untyped>`) beat prose every time | Enforce habits mechanically in the harness write path (e.g. rewrite `var/const X :=` → `= `); prompts persuade, harnesses prevent — same law, syntax edition |
| 34 | Model role-plays an agent: emits `<tool_call>`/`<function=...>` XML instead of blocks | Chat template flips into tool-calling mode (stacked thinking suppressors, or spontaneously at temp 0.7) | ONE suppressor only (the `</think>` prefill); temp 0.4; story states "you have NO tools; your reply is written straight to disk" |
| 35 | Story unwinnable by construction; loop burns iterations on a fight it cannot win | Two harness protections collided: verify targeted a file the snapshot-exclusion hides, greps scoped to a dir the file is not in | When adding any protection, re-read every verify it touches; the judge and the snapshot must agree on which files exist |
| 36 | "It works" (all gates green) but the user's first click does nothing | Boot/flow gates prove the app runs, not that its UI responds; menu buttons had no handlers at all | Anything a user can click gets a click-through gate (probe clicks the real button and asserts the state change); keyboard-first games need a controls card on screen or mouse parity — users click first |
| 37 | Manager feeds the loop a confident wrong diagnosis; model implements it perfectly, bug remains | Judge-side forensics asserted a plausible mechanism without reproducing it | Manager diagnoses bind by the same rule as gates: reproduce first (SSR-vs-DOM diff, unminified console, isolated prod), then name the culprit in the story |
| 38 | Every tmux command hangs forever; loops look dead but keep generating | The tmux SERVER process was SIGSTOPped (state `T` in ps) — clients queue on the socket silently; pane processes are children of the server and keep running unstopped | When tmux hangs, `ps -o stat -p $(pgrep tmux)` FIRST — a `T` state means stopped, not loaded; `kill -CONT` it. Never diagnose "machine overloaded" before checking server state |
| 39 | After un-wedging tmux, killed sessions resurrect and a loop you just paused restarts | Every client command queued against the stopped server (from ALL sessions — kills AND starts) executes at once on SIGCONT | Expect the backlog replay: after `kill -CONT`, immediately re-check sessions, processes, and `lms ps`; a clean check taken one minute earlier proves nothing |
| 40 | "Pause everything" doesn't stick: killed loops respawn, unloaded models reload | Chain/restart wrappers (fix-chain scripts, queued-start waiters, other manager sessions' queued commands) outlive their children and respawn them | Pause order: find respawners via ppid of anything that comes back, kill wrappers FIRST, then supervisors/claims, then iterations, then `lms unload --all` — and re-verify minutes later. Mechanical version: a `PAUSED` sentinel in the slots dir checked by claim.sh freezes all claims box-wide |
| 41 | A slot is held for hours by a loop doing nothing; queued loops starve | Slot reaping is pgrep-based — an alive-but-wedged supervisor (log stale 4.5h, model instance IDLE) is invisible to it; "process exists" ≠ "loop progressing" | Watchdog must also compare the holder's log mtime: alert the manager when a slot-holder's log is stale > 30 min while the queue is non-empty; parking at a checkpoint must RELEASE the slot (supervisor law), so a stale holder is always a bug worth a ping |
| 42 | Days of confident slop from every loop; reasoning_content near-empty | The `</think>` assistant prefill (old law 1) suppressed thinking entirely — fixed the empty-reply bug on the 8-bit quant but capped reasoning at zero | Thinking ON: no prefill, `reasoning_effort: xhigh`, `enable_thinking`/`preserve_thinking` true. Thinking off = garbage; this was the 2026-08 three-day bug |
| 43 | "Model is loaded" but output is slop | The 8-bit quant silently served API calls while the bf16 sat loaded — `lms ps` showed *a* model, not *which* | Check the `lms ps` identifier→model mapping, not just that something is loaded; pin QWEN_MODEL to the bf16 identifier |
| 44 | Every verify green, product is slop | Grep-tier verifies assert text exists, not that the thing works | Verifies must be BEHAVIORAL: browser click-through (playwright), headless sim run, measured numbers on failure (law 9) |
| 45 | Half-generated files on disk after a slow iteration | The harness wrote FILE blocks as they streamed; a truncated tail block hit disk mid-generation | Write files once, after the stream completes; mid-stream writes are LAST_REPLY/LAST_THINKING observability only |
| 46 | Iterations "hang" and get killed; replies arrive amputated | A 30-minute think at xhigh is NORMAL — timeouts and impatient humans killed healthy generations | Don't kill long thinks, budget them: chat timeout 5400s, dead-man threshold above the longest think |

| 47 | Camera renders from inside the terrain ("stuck in a prism"); physics insists the player is above ground | Eye-height constant applied TWICE: spawn/physics treat `player.y` as eye level, the render path subtracts EYE again (`eyeY = player.y - EYE`) | One convention per codebase: pick "player.y = eye" or "= feet" and grep every use. Gate: probe asserts camera eye-Y ≥ surfaceY+1.0 after spawn, and a screenshot check (below) |
| 48 | Player embedded in solid geometry; every move refused by collision; coords frozen | A stale localStorage save (new "Continue" feature) loaded a position into terrain that no longer matches; spawn/load paths never validated the position is clear | Spawn AND load must re-validate: place the player at the first non-solid column above the saved XZ. Gate: input-simulation probe — press W for 1s in a real browser, assert position CHANGED (probe-move.mjs) |
| 49 | Verify greps found `WASD|KeyW|movement` — the controls were completely dead | Code-existence is not function; the grep passed while the player could not move | Every control named in the HUD/controls card gets an input-simulation assertion: send the real key/click, assert the state delta. No grep gates for interactivity, ever |
| 50 | A relaunched loop dies minutes after starting for no visible reason | A stale timed killer from the manager's own session (`sleep 1800 && pkill <pattern>`) fired AFTER the relaunch and matched the new process | Timed killers must name their target pattern AND their own expiry; before relaunching, pgrep for leftover sleepers. Better: supervisors own the lifecycle (run_loop.sh); managers never arm bare timers |
| 51 | PATCH blocks sail past the write fences that FILE blocks respect | apply_patches checked only tools//scripts prefix — a patch could rewrite project.godot or strip protected symbols that a FILE write would have rejected | Fences must live in the shared path, not per-format: apply_patches now enforces allowedFiles + protect on post-patch content (found by pre-flight audit, not by a burned night) |
| 52 | "14/14 stories green" was fake — the shipped game had files that never even parsed | THREE stacked gate holes: (1) GUT `-gexit` quits but exits 0 with failing tests AND silently skips test files that fail to parse; (2) env.sh computed the project root one directory short (`../..` from 3-deep), so helper gates verified an EMPTY directory and exited 0; (3) the harness trusted `returncode == 0` only | Exit code is NEVER a gate for Godot/GUT: run_verify greps output for `SCRIPT ERROR|Parse Error|Failed to load|N failing tests` regardless of exit code (VERIFY_FAIL_MARKERS). And RED-TEST every gate after building/changing it: deliberately break something, watch it fail, revert. A gate you have never seen fail is a gate that does not work |
| 53 | Physics tests frozen solid; failures surface in an unrelated later suite (tractor "bug" was really a menu test's fault) | `SceneTree.paused` is global mutable state; booting main.tscn enters the menu and pauses the tree; GUT runs files alphabetically so the leak hits whatever runs next | Any test file that boots the main scene unpause-guards `after_each` (`get_tree().paused = false`); physics-dependent tests unpause in `before_each` as cheap insurance |
| 54 | Same suite passes/fails with identical code depending on test order | Under `-d`, a `SHADOWED_GLOBAL_IDENTIFIER` warning counts as an "Unexpected Error" attributed to whichever test triggers the first compile | Treat warnings as gate-relevant: `@warning_ignore` intentional shadows; when a failure flips with ordering, suspect shared state (paused tree, autoloads, compile-time warnings) before the test itself |
| 55 | Loop burns 4+ iterations on a story whose fix is one property name | Model prior trap: it assigns `.energy` on OmniLight3D — the Godot property is `light_energy` (same bug in the MCP lab AND the game loop) | Model engine-API priors beat docs. When a story touches engine APIs with misleading names, the story TEXT names the exact property/method; LAST_VERIFY already shows the error — if the model repeats it twice, tighten the story (law 6), don't wait for the block |
| 56 | Gate "worked" for weeks, first real break shows it never could fail | Gate was never red-tested after construction (exit-code trust, #52's root) | Every new gate ships with a permanent red-test FIXTURE (a deliberately-broken input kept in-repo) that CI/the audit runs against it — a gate that has never failed is not a gate |
| 57 | Model fails 6+ iterations on a simple story; story text explicitly names the fix and it still fails | SCOPE CREEP + stubborn prior: it invented a pod spin feature nobody asked for, on a hallucinated API (randf_vec3), and each iteration defended its invention instead of deleting it — the story said "this API doesn't exist" and it kept trying to make it exist | Story tightening has a second gear beyond "name the trap": name the INVENTED CODE and order its deletion by line ("your FIRST edit must delete X at line ~N"). If a model defends scope creep twice, the story orders the amputation |
| 58 | Model ruts for 6+ iterations per story — every rut is in the TEST file, not the feature | Law 10 under-applied: the model was asked to write both the feature AND its acceptance test, and test-authoring is where a 27B hallucinates framework APIs (parse errors, fake asserts, wrong arg counts) | Law 10 is load-bearing, not advisory: the MANAGER authors acceptance tests for any story touching a framework's assert API; the test file is NOT in allowedFiles so the model can't rewrite the spec to fit its code. The model writes game code to a test it cannot game |
| 59 | Manager-authored RED specs poison the whole run: every story's regression verify fails before it starts | Law 15 deployed naively: 13 manager tests committed to tests/ at once; the full-suite regression gate sees 12 RED specs for stories that haven't started and nothing can ever pass | Stage specs progressively: manager tests live in tests_staged/, each story's verify command does `cp tests_staged/<test> tests/` before running the scoped gate. RED specs enter the suite exactly when their story starts, and stay green after it passes. The regression gate grows with the run |
| 60 | File clobbered on disk despite the fence logging "skip: not in allowedFiles" — regression gate then deadlocks the story (model can't fix what it can't write) | WRITE PATH LEAK (root cause TBD): harness logged the fence rejection yet follow_cam.gd changed anyway; every write path audited appears fenced | Two defenses held: regression verify caught it, no commit landed. Manager unstick = curation restore (`git checkout -- <file>`), never let the model chase files outside its fences. OPEN: find the leaking write path — suspects: reasoning-fallback emission, architect call B, or an unlogged strip_markers write. Until found: any story whose regression fails on a file OUTSIDE its allowedFiles gets a manager restore, then continues |
| 61 | Manager's own acceptance test fails against CORRECT code; model burns iterations chasing a phantom | Boot-order bug in the manager-written test: `add_child` defers `_ready` to frame end, so `start_game()` ran BEFORE `_ready`, and `_enter_menu_state()` then stomped it — tree stayed paused in MENU, `_process`-driven UI never updated. Older tests never noticed (their asserts don't depend on _process-filled UI) | Acceptance tests that boot the main scene MUST `await get_tree().process_frame` after add_child BEFORE calling start_game. And law 10 cuts both ways: when a story ruts, re-verify the MANAGER's test with a standalone probe before blaming the model (#37 applies to us) |
| 62 | Suite suddenly reports "Could not find type X" across files that haven't changed | Rewrites silently dropped `class_name` declarations, and the stale global class cache MASKED it for hours (gates passed on cached registrations) until a later --import exposed it | Gates must run `godot --headless --import` before the suite after ANY git operation (commit/checkout) that changes file mtimes. And class_name belongs on the protect-list: a rewrite must never drop it (add to PROTECT symbols per file) |
| 63 | Iteration process dies mid-run; supervisor restarts it, then the SUPERVISOR dies silently (no exit line, slot held) | Recurring "tuple index out of range" crash in qwen_iteration.py (2x in one night, root cause TBD — likely an unguarded unpack in the trajectory/reply handling); supervisor death mechanism unknown — possibly the same crash in its own subprocess handling | run_loop's restart ladder works for the first crash; the SECOND death needs a manager watchdog (the 20-min monitor caught it: LOOP_NOT_RUNNING + slot held = relaunch). RESOLVED 2026-08-20: traceback armor named it — `safe_rel` did `parts[0]` on `Path(rel).parts` without an empty-check; a model-emitted `### FILE: .` yields `parts == ()` and the IndexError killed the iteration (and, propagated, the supervisor). Guard added (`if not parts: return None`) in all three copies + red-tested with `safe_rel(".")`. The armor pattern paid for itself: one crash with a traceback beat weeks of guessing |
| 64 | Iteration emits NOTHING: 200KB of thinking, zero visible reply, "no FILE blocks" failure | xhigh think spiral consumed the whole token budget on a JUICE story — reasoning_content filled, visible content empty | Track think_bytes/reply_bytes per iteration (trajectory has both): reply_bytes == 0 with think_bytes > 100KB is the spiral signature. The medium-effort A/B is no longer optional for juice-layer stories; also cap think growth per story class. Manager response per law 13(b): small feature, manager writes it, loop moves on |

| 65 | Story ruts 5+ iterations; every LAST_VERIFY is a wall of `Node './X' was modified from inside an instance, but it has vanished` followed by a null-instance crash in `_ready()` | Whole-file `.tscn` re-emission dropped `instance=ExtResource(...)`/`type=` off the root's children and wrote them as `[node name="Ship" parent="." index="0"]` — i.e. overrides of nodes that don't exist. Header `[ext_resource]` block was fully intact, so the scene *looks* correct at a glance and the diff reads as a harmless reorder | The `.tscn` fence is not enough on its own: rules must name the shape (`index="..."` on a root child = the bug) and the protect-list should treat `instance=ExtResource` on existing node lines as a required symbol, same as `class_name` (#62). Diagnostic shortcut for the manager: `git show HEAD:main.tscn | grep '^\[node'` vs `grep '^\[node' main.tscn` — a one-line structural diff names the rut in seconds, where the GUT log buries it under 200 lines of backtrace |
| 66 | Manager watched one story fail 5 identical iterations (~4h) with the root cause fully diagnosed after fail #2 — and only wrote a rules note, waiting for the model to self-correct | Law 13(b)'s "3+ times" threshold plus monitor-tier caution: each extra fail felt like "giving the rule a fair test", but a rules-file edit does not reach an iteration already in flight, and identical fail #2 already proves reproduction (#37's bar is met) | Law 13(b) trigger tightened to 2 consecutive same-cause fails (user directive 2026-08-20). Mechanical form: when `progress.txt` shows the same story + same LAST_VERIFY signature twice in a row, the manager intervenes NOW — kill the in-flight iteration if one is running blind on the old rules (supervisor respawns it), apply the minimal fix, log `managerInterventions`. Watching a diagnosed rut is not monitoring, it is spending GPU-hours to re-learn a known fact |
| 67 | Acceptance test unpassable by ANY correct implementation: story mandates a one_shot GPUParticles3D burst "at signal time" with process_mode ALWAYS + immediate tree pause, test polls `emitting` only AFTER awaiting physics frames | In headless (dummy rendering server) a one_shot ALWAYS-mode particle auto-clears `emitting` within ~1 frame (measured: 0-3 frames true of 12; INHERIT/PAUSABLE under a paused tree hold it indefinitely) — the flag is only observable synchronously, the moment the signal handler returns | When the story says "at signal time", the test must assert at signal time: check state synchronously right after the triggering call, THEN optionally poll. Second instance of #61's law — when a story ruts, probe the manager's test against a hand-written correct implementation before burning more model iterations; here 5 iterations died on a scene bug (#65) that was masking a test bug behind it |
| 68 | .tscn parse error at a Color line; second fail surfaced as a null `.color` access; a styling regression then failed on colors that "look right" | Three-arg `Color(r, g, b)` is valid GDScript but INVALID in .tscn text format (the resource parser demands all 4 components); and the model rounds channel floats (0.961 for 245/255=0.960784) while banked styling tests assert `is_equal_approx` on exact values — rounded literals in scene AND in a runtime `const` both fail it | .tscn Color literals always carry 4 components; brand-palette colors are copied to full float precision from the test/brief, never re-derived by eye. Both belong in the traps list; a styling test's expected values should be quoted verbatim in the story text |
| 69 | Fresh project's every iteration fails identically; LAST_VERIFY contains no test output at all, only a python "can't open file" line | The repo's reference/ harness bundle was INCOMPLETE — strip_markers.py was only ever in the original project's scripts/ralph/, so every new project cloned from reference/ dies in the write pipeline before any gate runs | reference/ now carries strip_markers.py. Law for new-project setup: after copying reference/, run ONE end-to-end dry iteration (or diff reference/ against the last working project's scripts/ralph/) BEFORE launching overnight — a missing harness file fails every iteration with the same non-test error, which is the tell (real ruts fail inside the gates, harness holes fail before them) |
| 70 | Combat story fails repeatedly with correct-looking damage logic; GUT reports only "Unexpected Errors" | `set_deferred("monitoring", true)` + a same-frame `get_overlapping_areas()` — the deferred set lands at frame END, so the query runs with monitoring still off and the engine error trips GUT's error gate (#54 family: engine errors ARE failures under -d) | Guard every overlap query with the property it depends on (`if hitbox.monitoring:`). Trap-list line for 2D fighters: deferred physics-state sets are invisible until frame end — never query in the same frame you set_deferred |
| 71 | Story adding a new `class_name` fails the PARSE gate with "Could not find type X" though the code is valid; gut gate (import-wired) would have passed it | #62's fix was wired into verify-gut only — verify-parse runs FIRST and did per-file --check-only against a stale class cache, so the model gets punished for the gate's own blindness | Every Godot gate that reads .gd files runs `--headless --import` first, not just the suite gate. Meta-rule: when you fix a cache/staleness bug in ONE gate, grep the other gates for the same dependency before calling it fixed |
| 72 | Two "model" failures on an AI story; the model's brain code was flawless both times | Both were manager-test spec bugs (#61 twice in one story): (a) asserting a TRANSIENT state ('approach') at a fixed late frame — by then the AI had legitimately advanced to 'attack'; (b) a symmetric combat fixture (defender punches, attacker kicks, equal reach) is a mutual-hit RACE — whoever's active frames come first stuns the other, and any timing drift flips the outcome | Fighting-game test law: sample transient states EARLY (frames into the behavior, not after it), and make combat fixtures asymmetric-by-construction (slow move vs fast move) so exactly one hit can land. A test that encodes a race passes until an unrelated rewrite shifts one frame |
| 73 | Supervisor silently gives up mid-run ("5 crashes — giving up") on a healthy loop; every "crash" was rc=143 | The manager's own interventions pkill the in-flight iteration (correct move under the 2-fail rule), but run_loop counted each SIGTERM as a crash and exhausted MAX_RESTARTS | run_loop treats rc=143 as an intentional kill: respawn without incrementing the ladder. General law: when the manager's standard playbook interacts with a supervisor's failure accounting, the supervisor must be able to tell intervention from crash — otherwise following the playbook slowly kills the loop |
| 74 | 4-file juice story: two consecutive emissions mangled — reasoning prose + an unapplied PATCH block written INTO a .gd body (stray ``` fence at line ~300), a .tscn corrupted at line 1 | #5's planning-prose filter catches prose at block START but not a fence-close mid-body followed by prose+PATCH text — the applier trusted the block to its end marker. Root correlation: emission quality collapses with file COUNT per story (1-2 files fine all run; 4 files mangled twice) | Two fixes: (a) applier should reject any FILE body containing a bare ``` line or '### PATCH:' text (fence-leak scanner); (b) story-authoring law: keep stories ≤2 emitted files; a 4-file feature is 2 stories (scene+mechanics, then wiring). Manager response when it bites anyway: implement whole (13b), don't retry the model on a story shape it has failed twice |
| 75 | Late story fails on EARLIER stories' regression tests though its own feature works | The PRD itself schedules flow changes (US-015 inserts a banner beat before round activation) that earlier manager tests pinned as immediate — the spec suite disagreed with its own roadmap | When authoring the test set up front, scan later stories for flow changes they will impose on earlier asserts and write the earlier tests tolerance-first (await-with-window instead of assert-at-frame). A pinned timing that a scheduled story will legally change is a time bomb the manager planted |
| 76 | Run declared COMPLETE at green-suite + clean smoke; first human look: "looks like shit". Every mechanic worked; the screen was empty black, fighters postage-stamp small, no camera, no arena dressing | The critique phase (CRITIC.md) exists but was never wired into the definition of done — the manager stopped at the mechanical gates and never rendered a frame. A suite with zero visual assertions gates zero visuals | "Done" for a game = green suite + clean smoke + **critique pass**: capture canonical frames (shoot instrument), judge them against the BRIEF's look section, file every flaw as a story, and let the LOOP fix them — the model writes the polish, not the manager. MANAGER.md finishing checklist amended; a run that skips critique is not finished, it is abandoned at "functional" |
| 77 | 18h run shipped a "complete" game that the user called clipart slop; every gate green, art never considered | The MANAGER wrote programmer art into the spec: the BRIEF said "fighters built from Polygon2D limbs", and when adapting QWEN.md from the previous project the manager DELETED its anti-slop line ("no primitive-mesh placeholders where a Kenney GLB exists") and replaced it with polygon instructions. ASSETS.md (CC0 sources) and MODELING.md (Blender authoring) both existed and went unused; the machine had rigged CC0 characters with 76 animations sitting on disk the whole time | Law 16 + `reference/preflight_assets.sh`, wired into run_loop.sh so it BLOCKS launch until real licensed art is on disk and the brief names it. Guidance that lives only in a doc gets skipped under momentum — a rule you care about must be an executable gate. Corollary: never edit an anti-slop rule OUT of a template; template adaptation is where guards die quietly |
| 78 | Quality gate rejects a correct, fully-specified story description as "placeholder text" | `PLACEHOLDER_RE` included a bare `<[^>]+>`, so ANY prose containing a `<` and a later `>` matched — e.g. "z_index < 0 ... (r+g > 1.2)". Precise specs use comparisons constantly, so the gate punished exactly the descriptions it was built to demand | Require a letter/underscore/slash immediately after `<` and forbid whitespace inside: `<[A-Za-z_/][^<>\s]*>` matches `<name>` / `<path/to/x>` but not a comparison. Red-tested both ways. General law: a "placeholder detector" that can fire on legitimate content trains the manager to soften real specs to appease the gate — the exact opposite of the gate's purpose |
| 79 | Loop dies with "5 crashes — giving up"; every iteration failed `Qwen API failed: HTTP Error 400`; the 45-min heartbeat showed `lms=200` the entire time | The loop's model identifier had been EVICTED from the server (a co-tenant's ensure-job reloaded its own set), so requests named a model that no longer existed. `GET /v1/models` still returned 200 because the SERVER was healthy — the heartbeat was asking the wrong question, and a simple curl even "worked" by falling back to whatever model was loaded | The route check must assert YOUR identifier, not server liveness: `curl -s /v1/models \| grep -q "<identifier>"`. This is #32 ("confirm the identifier maps to the weights you think") reappearing as a MONITORING failure rather than a setup one. On a shared box also expect eviction mid-run: reload-and-relaunch is the manager move, and the heartbeat must be able to say "server up, my model gone". RESOLVED 2026-08-23 after a second eviction: `reference/ensure_model.sh` runs inside run_loop before EVERY round — it checks the identifier and reloads it additively (never unloading a co-tenant's instance), so eviction costs one round's pause instead of the whole ladder |
| 80 | Third manager-test-is-unpassable in this catalog (#61, #67, now #80): the spec drove an isolated rig while the feature correctly lived in the game shell, and a live AI opponent fired the very effect under test underneath the assertions | Two distinct spec faults with one root: the test's WORLD did not match the story's allowedFiles (rig without the shell that owns global state like Engine.time_scale), and the fixture contained an autonomous actor. The model's implementation was correct both times | When authoring a spec, ask "which file does the story let the model edit, and is that file even INSTANTIATED in my fixture?" — if the feature lives in the shell, boot the shell. And silence autonomous actors (AI brains, spawners) in any fixture that asserts timing; a live opponent makes the clock non-deterministic. Add both to STORY-CHECKLIST |
| 81 | A story fails twice with the model producing sane work each time; every failing assertion names a file the story's `allowedFiles` forbids | The manager wrote ONE story spanning four owners (HUD layout + camera framing + menu button) but fenced it to two files. Law 17's "<=2 emitted files" was satisfied on paper while the ACCEPTANCE TEST demanded four — the fence and the spec disagreed, so no correct emission could ever pass | Before launching a story, cross-check every assertion in its test against `allowedFiles`: if an assertion can only be satisfied by a file the model may not write, the story is mis-scoped — split it by OWNERSHIP (one story per owning file-pair) and split the spec the same way. Corollary to #80: the fixture must instantiate what the story targets, AND the assertions must only demand what it may edit |
